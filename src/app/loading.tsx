/**
 * Renders a loading indicator component.
 * @returns {string} A simple "Loading..." text as a placeholder.
 */
export default function Loading() {
  // Or a custom loading skeleton component
  return "Loading..."
}
