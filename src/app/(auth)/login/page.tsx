import { LoginForm } from "../components/LoginForm"

/**
 * Renders the login page component
 * @returns {JSX.Element} A React component that displays the login form
 */
export default function LoginPage() {
  return <LoginForm />
}
